package com.treefrogapps.mediasessionplayer.MusicPlayer.MusicService.MediaSession;

import android.content.Context;
import android.content.Intent;
import android.support.v4.content.WakefulBroadcastReceiver;

/**
 *  MediaSessionReceiver
 */
public class MediaSessionReceiver extends WakefulBroadcastReceiver {


    @Override
    public void onReceive(Context context, Intent intent) {

        // TODO - Handle MediaButton (add to manifest or dynamically - doze mode effect manifest/static intent filter?)
    }
}
